---
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
import { SITE_TITLE } from '../../consts';

const posts = (await getCollection('blog'))
	.filter(p => p.data.category !== 'フォトログ')
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<!doctype html>
<html lang="ja">
	<head>
		<BaseHead title={`記事一覧 | ${SITE_TITLE}`} description="すべての記事一覧" />
	</head>
	<body>
		<Header />

		<div class="page-hero">
			<div class="page-hero-inner">
				<h1 class="page-hero-title">記事一覧</h1>
				<p class="page-hero-sub">{posts.length} 件の記事</p>
			</div>
		</div>

		<main class="blog-main">
			<ul class="post-list">
				{posts.map((post, i) => (
					<li class={`post-card reveal ${i === 0 ? 'post-card--hero' : ''}`}>
						<a href={`/blog/${post.id}/`}>
							{post.data.heroImage && (
								<div class="card-img">
									<img
										src={post.data.heroImage}
										alt={post.data.title}
										loading="lazy"
										transition:name={`hero-${post.id}`}
									/>
								</div>
							)}
							<div class="card-body">
								<h2 class="card-title">{post.data.title}</h2>
							</div>
						</a>
					</li>
				))}
			</ul>
		</main>

		<Footer />
	</body>
</html>

<style>
.page-hero {
	background: linear-gradient(135deg, var(--color-bg) 0%, var(--color-bg-alt) 100%);
	border-bottom: 1px solid var(--color-border);
	padding: 3.5rem 0 3rem;
}
.page-hero-inner {
	max-width: 1100px;
	margin: 0 auto;
	padding: 0 var(--sp-6);
}
.page-hero-title {
	font-family: var(--font-serif);
	font-size: var(--fs-2xl);
	margin: 0 0 0.3rem;
}
.page-hero-sub {
	font-size: var(--fs-sm);
	color: var(--color-text-muted);
	margin: 0;
	letter-spacing: 0.05em;
}

.blog-main {
	max-width: 1100px;
	margin: 0 auto;
	padding: var(--sp-7) var(--sp-6) var(--sp-8);
}

.post-list {
	list-style: none;
	margin: 0;
	padding: 0;
	display: grid;
	grid-template-columns: repeat(3, 1fr);
	gap: 1.5rem;
}

/* hero (first) card — full-bleed photo with text overlay */
.post-card--hero {
	grid-column: 1 / -1;
	position: relative;
	height: 360px;
	overflow: hidden;
}
.post-card--hero > a {
	display: block;
	position: relative;
	height: 100%;
	text-decoration: none;
	color: inherit;
}
/* 画像：カード全体を覆う */
.post-card--hero .card-img {
	position: absolute;
	inset: 0;
	overflow: hidden;
}
.post-card--hero .card-img img {
	width: 100% !important;
	height: 100% !important;
	object-fit: cover !important;
	border-radius: 0 !important;
	filter: saturate(0.82);
	transition: transform .5s ease, filter .4s !important;
}
.post-card--hero:hover .card-img img {
	transform: scale(1.03);
	filter: saturate(1);
}
/* グラデーションオーバーレイ */
.post-card--hero::after {
	content: '';
	position: absolute;
	inset: 0;
	background: linear-gradient(to top, rgba(0,0,0,0.72) 0%, rgba(0,0,0,0.22) 55%, transparent 100%);
	pointer-events: none;
	z-index: 1;
	border-radius: var(--radius-lg);
}
/* テキスト：画像下部に重ねる */
.post-card--hero .card-body {
	position: absolute !important;
	bottom: 0;
	left: 0;
	right: 0;
	z-index: 2;
	padding: 2rem 2.5rem !important;
	background: none !important;
}
.post-card--hero .card-title {
	font-size: var(--fs-xl) !important;
	margin-bottom: 0.6rem !important;
	color: #fff !important;
	line-height: 1.4 !important;
}
.post-card--hero:hover .card-title {
	color: #fff !important;
}

/* normal card */
.post-card {
	background: var(--color-surface);
	border: 1px solid var(--color-border);
	border-radius: var(--radius-lg);
	overflow: hidden;
	transition: box-shadow .25s, transform .25s;
}
.post-card:hover { box-shadow: var(--shadow-lg); transform: translateY(-2px); }
.post-card > a, .post-card:not(.post-card--hero) > a {
	display: block;
	text-decoration: none;
	color: inherit;
	height: 100%;
}
.card-img { overflow: hidden; }
.card-img img { width: 100%; height: 200px; object-fit: cover; display: block; filter: saturate(0.82); transition: transform .4s, filter .4s; border-radius: 0; }
.post-card:hover .card-img img { transform: scale(1.04); filter: saturate(1); }

.card-body { padding: 1.2rem 1.4rem 1.5rem; }
.card-title { font-family: var(--font-serif); font-size: var(--fs-md); font-weight: 700; color: var(--color-text); margin: 0 0 0.55rem; line-height: 1.5; }
.post-card:hover .card-title { color: var(--color-accent); }

@media (max-width: 900px) {
	.post-list { grid-template-columns: repeat(2, 1fr); }
	.post-card--hero { height: 280px; }
	.post-card--hero .card-body { padding: 1.5rem 1.75rem !important; }
	.post-card--hero .card-title { font-size: var(--fs-lg) !important; }
}
@media (max-width: 600px) {
	.blog-main { padding: var(--sp-6) var(--sp-4) var(--sp-7); }
	.post-list { grid-template-columns: 1fr; }
}
</style>
